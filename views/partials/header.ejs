<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Trade Me!!!!</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <div class="headerdiv">
            <a href="/"> <img id="logo" src="/images/logo.png" alt=" Trade Me" width="130" height="130"> </a>
                <h1 class="heading1">Trade Me</h1>
                <% if(errorMessages.length > 0) {%>
                    <% errorMessages.forEach(message=>{%>
                        <div class='error'>
                            <%=message%>
                        </div>
                        <%});%>
                            <%}%>
                
                                <% if(successMessages.length > 0) {%>
                
                                    <% successMessages.forEach(message=>{%>
                                        <div class='success'>
                                            <%=message%>
                                        </div>
                                        <%});%>
                                            <%}%>
                <nav>
                    <p class="usernav">
                        <a  href="/trades/new">Start Trading! &nbsp;</a>
                        <%if(!user){%>
                            <a href='/users/new'>Sign Up</a>
                            <a href='/users/login'>Login</a>
                        <%} else {%>
                            <a href='/users/profile'>Profile</a>
                            <a href='/users/logout'>Logout</a>
                        <%}%>   
                    </p>
                </nav>
            <hr id="breakinheader" >
            <nav>
            <a href="/trades" >Trades</a>
           
            </nav>
            
        </div>
        
    </header>
    